# Zero-backend dev mode: `vite --mode fixtures` (see .claude/launch.json).
# Serves the in-memory dataset in src/mocks/ instead of fetching /api/*, so the
# UI can be run and reviewed without Postgres, Hyperdrive, or the Worker.
# No secrets here — this file is committed deliberately.
VITE_USE_FIXTURES=1
