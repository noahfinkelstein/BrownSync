(() => {
  const groups = [{"group":"code:AMST 2922","srcdb":"202610","sections":[{"crn":"10225"},{"crn":"10226"},{"crn":"10227"},{"crn":"10228"},{"crn":"10229"},{"crn":"10230"},{"crn":"10231"}]},{"group":"code:AMST 2923","srcdb":"202610","sections":[{"crn":"10232"},{"crn":"10233"},{"crn":"10234"},{"crn":"10235"},{"crn":"10236"},{"crn":"10237"},{"crn":"10238"}]},{"group":"code:AMST 2950","srcdb":"202610","sections":[{"crn":"10239"},{"crn":"10240"},{"crn":"10241"}]},{"group":"code:AMST 2990","srcdb":"202610","sections":[{"crn":"13275"}]}];
  const extractGroup = (group) => {
    const groupRows = [];
    return group.sections
      .reduce(
        (previous, section) =>
          previous.then(() => {
            const key = "crn:" + section.crn;
            return fose.detailsAPI
              .fetchFor(group.group, key, key, group.srcdb)
              .then((detail) => {
                const meetingContainer = document.createElement("div");
                meetingContainer.innerHTML = detail.meeting_html || "";
                groupRows.push({
                  crn: String(detail.crn || section.crn),
                  course_code:
                    detail.code || group.group.replace(/^code:/, ""),
                  meeting_html: detail.meeting_html || "",
                  schedule_and_location: (meetingContainer.textContent || "")
                    .replace(/\s+/g, " ")
                    .trim(),
                });
              })
              .catch((error) => {
                groupRows.push({
                  crn: String(section.crn),
                  course_code: group.group.replace(/^code:/, ""),
                  meeting_html: "",
                  schedule_and_location: "",
                  error: String(error),
                });
              });
          }),
        Promise.resolve(),
      )
      .then(() => groupRows);
  };

  const starts = Array.from(
    { length: Math.ceil(groups.length / 4) },
    (_, index) => index * 4,
  );
  const rows = [];

  return starts
    .reduce(
      (previous, start) =>
        previous
          .then(() =>
            Promise.all(groups.slice(start, start + 4).map(extractGroup)),
          )
          .then((groupRows) => rows.push(...groupRows.flat())),
      Promise.resolve(),
    )
    .then(() => JSON.stringify(rows))
    .catch((error) =>
      JSON.stringify({
        top_error: String(error),
        stack: error && error.stack ? String(error.stack) : "",
      }),
    );
})()
