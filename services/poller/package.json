{
  "name": "@brownsync/poller",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Structured-feed ingestion workers (LiveWhale JSON, athletics ICS, BDH RSS) per DATA_CONTRACT.md §2/§5. Python scrapers live in ingest/ (Codex-owned) — not here.",
  "scripts": {
    "poll": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@brownsync/contract": "workspace:*",
    "fast-xml-parser": "^5.10.1",
    "node-ical": "^0.27.1",
    "postgres": "^3.4.9",
    "zod": "^4.4.3"
  }
}
