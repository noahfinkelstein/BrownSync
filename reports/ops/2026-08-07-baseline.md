# Ops baseline — 2026-08-07 (launch orchestration, Phase 0)

Session: ops-deck launch handoff (see `handoffs/` + commit `efa7bd1`).

## Dirty-tree decision (Phase 0 step 1)

At handoff time `main` was dirty with a large in-flight change (~23k insertions in
modified files + ~100 untracked files): the **social lane** — board
(identity/queries/routes/account-deletion/G6 guards), org assets, org
admin/enrichment, user events — spanning `apps/api`, `packages/contract`
(social.ts/board.ts/org-assets.ts + regenerated openapi.json), migrations
`0013–0017` with matching `db/checks`, and a substantial iOS app expansion with
phase RED suites.

**Decision: commit, not stash.** The full suite passed with the in-flight work in
place (see baseline below), the work is coherent and test-covered, and stashing
23k lines including untracked files is the riskier path for data loss. Landed as
`251a06d feat(social): land in-flight board/org-assets/user-events lane`.

Notes:
- `_to_delete/` (stale git `HEAD.lock`/`index.lock` moved out of `.git`) left
  untracked deliberately; harmless, can be removed later.
- Migrations 0013–0017 are **not yet applied to prod**. Deploy workflow ships
  web only on main push; the API worker deploys manually per DEPLOY.md, so the
  in-flight API code cannot reach prod accidentally.
- Workstream E's adversarial review is the merge-quality gate over this landed
  code (especially the auth/RLS surface).

## Test baseline (Phase 0 step 2) — all green

| Suite | Result |
|---|---|
| `packages/contract` | 89 passed |
| `packages/ui` | 21 passed |
| `services/poller` | 133 passed, 17 skipped |
| `db` | 14 passed |
| `apps/api` | 643 passed (29 files, incl. board/org/user-event suites) |
| `apps/web` (unit) | 650 passed (58 files) |
| `ingest` pytest | 1412 passed, 98 skipped (postgres-integration skips: `TEST_DATABASE_URL` unset locally) |
| `tsc --noEmit` (all packages incl. web e2e project) | clean |

Web e2e: not run in this baseline pass (needs dev server/Playwright session);
will run as part of Workstream A's reproduce step and be recorded there.

## Prod before-state (Phase 0 step 3)

`GET https://brownsync-api.noahfinkelstein.workers.dev/api/health` at ~17:35Z:

| source | status | lastOkAt | note |
|---|---|---|---|
| athletics | ok | 2026-07-29 | **stale 9d** |
| athletics_ics | ok | 2026-08-07 17:27 | fresh |
| bdh | ok | 2026-08-07 17:27 | fresh |
| buildings | ok | 2026-07-29 | **stale 9d** |
| cab | ok | 2026-07-29 | **stale 9d** |
| clubs | ok | 2026-07-29 | **stale 9d** |
| dedup | ok | 2026-08-07 17:27 | fresh |
| events | ok | 2026-07-29 | **stale 9d** |
| livewhale | **partial** | **null** | runs every ~30min, upserts 1000, never reports ok — 1000-row-cap sharding fix NOT effective in prod |
| places | ok | 2026-07-29 | **stale 9d** |

Web: `https://brownsync.pages.dev` → HTTP 200. Map screenshots (3 cameras)
deferred into Workstream A's deterministic reproduce step.

### Staleness root-cause hypothesis (for Workstream B)

GitHub Actions: `Poll` runs green on schedule (many runs today); `Seed Load`
last materially ran ~Jul 29. Workflows present: CI, Deploy, Poll, Seed Load —
**no dining/refresh cron exists** (handoff's `refresh.yml` reference does not
match reality). The 6 stale sources are one-time seed loads with no refresh
path; the Poll lane only covers athletics_ics/bdh/dedup/livewhale. Permanent
fix remains the `source_registry`-driven dispatcher (Lane B keystone).
Separately: livewhale's prod `partial` needs investigation — the date-window
sharding fix is landed in tests but not effective (or not deployed) in prod.

## Measured-against

Every later workflow in this launch effort measures against this baseline:
zero test regressions; prod health must only improve.
