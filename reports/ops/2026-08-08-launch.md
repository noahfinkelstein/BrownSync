# BrownSync launch — 2026-08-08

Terminal report of the ops-deck launch effort (sessions of 2026-08-07/08;
prior detail in `2026-08-07-baseline.md` and `2026-08-07-launch-session-1.md`).

## Launch checklist (handoff §10)

- [x] **Phase 0** — dirty tree resolved (social lane landed green), baseline
  recorded, prod before-state captured.
- [x] **Map** — page-token leaks fixed (`c9a4a88`); cartography e2e with
  pixel-stat bounds + rendered-feature positive control, red-test verified;
  contrast/style/perf suites green; deployed.
- [x] **Data** — livewhale reports `ok` in prod (sharded, 1307 items);
  registry-driven Worker dispatcher live (`* * * * *`); refresh.yml active;
  health honest (per-source thresholds, `never`, paused-by-design); daily
  freshness audit routine installed, **fix-capable** (repo attached, PR-on-
  finding), observed firing. 7-day within-threshold window: **day 1 of 7,
  time-gated by definition** — the audit routine reports daily.
- [x] **Sources** — `brown_news` live end-to-end: vetted → registry row →
  recorded 43-item fixture → fail-closed producer (200-char headline cap,
  value-closed raw allowlist) → dispatcher claim → 43 items in prod →
  rendered in the feed with attribution + click-through. BDH headline-only
  interim per the recorded G1 owner decision (migration 0022). BDH
  permission email + Reddit application drafted for owner. Refusals
  recorded (providence_gov, today_brown, Sidechat). Major-outlet feeds:
  deferred post-launch (each vets in its own producer PR).
- [x] **UI** — audit → 8 fixes → freshman-lens acceptance PASS; round-2
  review regressions fixed (mobile presets restored, stable readout);
  polish landed (tooltip, phrasing, attribution clearance).
- [x] **Architecture** — register gaps closed (§2, §7, §8 previously, P0);
  two adversarial review rounds: round 1 (14 agents) → 4 confirmed P1s all
  fixed + released (migration 0019, contract v1.8); round 2 (8 agents) →
  2 P1s fixed same-day. P2 backlog (~30 items) triaged in the session-1
  report addenda. Standing red 0004 check cleared (migration 0020).
- [x] **Legal** — G1–G8 re-reviewed against everything shipping; G4/G5
  attribution strings verified on artifacts; G1 interim posture owner-
  approved and registry-recorded; nothing crawls the refused hosts.
- [x] **Deploy** — prod DB at full migration parity (0001–0023, includes
  articles RLS), Worker `1b6a540e`, Pages auto-deploying from main; CI
  fully green including the once-permanently-red migrate job.
- [x] **Prod verification** — real-browser render of brownsync.pages.dev:
  no error boundary, map boots, feed serves BROWN NEWS headlines "2 d ago";
  `/api/health`, `/api/events`, `/api/articles`, `/api/meetings` all 200.

## Deferred post-launch (recorded, owned)

- livewhale Worker migration + poll.yml retirement (release-gate test flips
  in that PR); Lane B producers (dining/libcal/arcgis/publications);
  major-outlet + Google News discovery feeds; r/brownu producer (ships
  disabled pending G8); BDH `articles` migration out of `events`.
- P2 backlog from both review rounds (session-1 addenda) — includes the
  TOCTOU on the sole-owner 409 (fix with board launch-gate work), seed-lane
  staleness thresholds (athletics/buildings/cab/clubs/events/places carry
  NULL thresholds and read stale-amber against the 45-min fallback — seed
  real thresholds or refresh cadence), account.test.ts CI flake (one
  occurrence, self-cleared).
- Owner queue: send the BDH email; file the Reddit application; the
  12-hour-clock design-law question; board launch gates when ready
  (pepper, limiter binding, initial owner, BOARD_ENABLED flip — sticky).

## Sign-off

Owner sign-off on the launch itself: requested 2026-08-08.
