# BrownSync freshness audit — 2026-08-24

Daily freshness audit, 17th consecutive session. No new code defect found —
today reproduces the exact two blockers already diagnosed and filed
(#21 code fix, #25 repo-setting finding). This report documents state only;
no 18th duplicate PR opened for either.

## Per-source table

| source | status | age vs threshold | verdict |
|---|---|---|---|
| livewhale | ok | 15m / 40m | FRESH |
| bdh | ok | 1h38m / 2h | FRESH |
| athletics_ics | ok | 1h38m / 4h | FRESH |
| brown_news | ok | 24m / 2h | FRESH |
| dedup | ok | 10m / 1h | FRESH |
| athletics | ok | 25.9d / null→7d default | **STALE** (seed lane, fix drafted in #12/#19/#20) |
| buildings | ok | 25.9d / null→7d default | **STALE** (seed lane) |
| cab | ok | 25.9d / null→7d default | **STALE** (seed lane) |
| clubs | ok | 25.9d / null→7d default | **STALE** (seed lane) |
| events | ok | 25.9d / null→7d default | **STALE** (seed lane) |
| places | ok | 25.9d / null→7d default | **STALE** (seed lane) |
| academic_calendar | never | — | NEVER — `refresh.yml` blocked (pytest gate, fix in #21) |
| dining | never | — | NEVER — same blocker |
| libcal | never | — | NEVER — same blocker |
| bpr | never | — | NEVER — same blocker |
| bjwa | never | — | NEVER — same blocker |
| ppl | never | — | NEVER — same blocker |
| arcgis | never | — | NEVER — repo Settings→Actions PR-creation toggle (see #25), weekly leg otherwise clears cleanly |
| passiogo | never | — | NEVER — producer not yet deployed (pre-existing, out of scope) |
| providence_gov / today_brown / feed_rank | n/a | disabled | paused by design |

## Workflow health

- **Poll**: green — last 30 scheduled runs all `success` (livewhale/15min, athletics+bdh/2h, dedup/hourly all firing on schedule).
- **CI**: green on the open PR heads.
- **Deploy**: green; still sitting on the 2026-08-08 launch commit — **zero merges to `main` in 16 days**.
- **Refresh data artifacts**: red — daily group (dining/libcal/publications/academic_calendar/bpr/bjwa/ppl) has now failed **20/20** scheduled runs since launch, always at the same `pytest` step, root-caused and fixed on-branch in #21 (`mergeable_state: clean`, still a draft, still unmerged). The Sunday-only `arcgis` leg clears its own gate cleanly but dies at `gh pr create` — blocked by the repo's Settings → Actions → General → "Allow GitHub Actions to create and approve pull requests" toggle, not code (per #25).
- **60-day cron auto-disable**: not yet triggered (16 days of inactivity on `main`, all 6 workflows report `state: active`), but this is the wrong kind of margin to be building — every day without a merge is a day closer.

## Web + data currency

- `brownsync.pages.dev` → 200.
- `/api/events`, next 7 days (2026-08-24 → 2026-08-31): 120 events, 104 active / 16 canceled — **all 120 from `livewhale`**. The one-time `events`-source seed (1,106 items loaded 2026-07-29) has now fully rolled past the lookahead window with nothing behind it to replace it, since `events`/`athletics`/`cab`/`clubs`/`buildings`/`places` have had no scheduled refresh since launch. Functionally this endpoint is healthy today (120 events, GREEN by the letter of the check), but it is quietly running on a single producer — a second-order symptom of the same seed-lane gap flagged since #12.

## Backlog

**15 open PRs (#11–#25), all from daily freshness-audit sessions, none merged, none closed.** Several are supersede-candidates for the same seed-lane fix (#12, #19, #20) — an owner triage pass picking one and closing the other two would shrink this by two immediately. #21 is the tested, `mergeable_state: clean` fix for the daily refresh red streak.

## Single most important next action

Merge **#21** (clears the 20/20 daily `Refresh data artifacts` red streak) and flip **Settings → Actions → General → "Allow GitHub Actions to create and approve pull requests"** (clears the `arcgis` leg). Both are zero-risk, already-verified, and together resolve the majority of the open backlog's root causes. Everything else in the 15-PR queue is a triage/merge decision, not new engineering.

## Overall verdict: AMBER

Live/polled sources, the web app, and today's upcoming-events count are all healthy on paper. The file-based ingest lane and the seed lane have been dark since the 2026-08-08 launch with tested fixes sitting unreviewed — 16 days and 15 PRs of review latency, not missing code, is what's standing between this repo and green.

---
_Generated by [Claude Code](https://claude.ai/code)_
