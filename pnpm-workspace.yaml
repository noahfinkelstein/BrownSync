packages:
  - "apps/*"
  - "packages/*"
  - "services/*"
  - "db"

allowBuilds:
  esbuild: true
  supabase: true
  "@tailwindcss/oxide": true
  "@biomejs/biome": true
  # msw's postinstall only prints a sponsorship banner; never run it.
  msw: false
